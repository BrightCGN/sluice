# /etc/sluice/gateway-openai.env — Instanz-Konfiguration für sluice-gateway@openai
# (§7.3, Rev. 6). chmod 600, Eigentümer root. Diese Instanz hält NUR den OpenAI-Key.
# SLUICE_HOST anpassen, wenn das Gateway auf einen eigenen Server umzieht.
SLUICE_HOST=192.168.87.40
SLUICE_PORT=17891
SLUICE_OPENAI_API_KEY=
# Optionales Shared Secret — muss dann auch im Kern (sluice.env) gesetzt sein:
# SLUICE_GATEWAY_TOKEN=
